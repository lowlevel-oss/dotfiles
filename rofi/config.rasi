configuration {
  modes: [ combi ];
  combi-modes: [ window, drun, run ];
}

@theme "gruvbox-light"

entry {
    placeholder: "Search";
    cursor: pointer;
}

element{
    orientation:horizontal;
    children:[element-text,element-icon];
    spacing:5px;
}

element-icon{
    size:1.5em;
}